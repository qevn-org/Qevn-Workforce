# Shared Library package

Contains internal schema definitions (Pydantic models) and database helper clients (PostgreSQL RLS-aware managers, Redis clients) shared across the Gateway, Orchestrator, and workers.
