# Tool Registry Package

Defines the base `BaseTool` class implementing argument validation, authentication verification, sliding-window rate limiting, exponential backoff retries, and execution log tracing. Also hosts SaaS modules (Slack, Gmail, HubSpot, Twilio, Google Calendar) and the dynamic `ToolRegistry` class.
